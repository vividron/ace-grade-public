<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Set the background color -->
    <solid android:color="@color/faint_blue" />

    <!-- Add a border (optional) -->
    <stroke
        android:width="2dp"
        android:color="@color/dark_blue" />
</shape>